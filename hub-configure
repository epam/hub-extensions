#!/bin/bash -e
# shellcheck disable=SC2046,SC2068

HUB=${HUB:-hub}

if test -n "$HUB_TOKEN"; then
  $HUB configure api $@
else
  # TODO: we need properly detect eks cluster
  $HUB configure eks $@
fi
